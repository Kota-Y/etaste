import { withRouter } from "react-router";
import React from "react";

class Profileset extends React.Component {
  
  render() {
    return (
      <div>
        <h1>プロフィール設定画面です</h1>
      </div>
    );
  }
}

export default withRouter(Profileset);
